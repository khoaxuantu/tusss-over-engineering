model CityTable {
  id   String @id @db.VarChar(32)
  name String @db.VarChar(32)

  stores StoreLocationTable[]

  @@map("cities")
}

model DistrictTable {
  id   String @id @db.VarChar(32)
  name String @db.VarChar(32)

  stores StoreLocationTable[]

  @@map("districts")
}
