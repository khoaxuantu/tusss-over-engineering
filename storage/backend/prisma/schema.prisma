datasource db {
  provider = "postgresql"
  url      = env("DB_URL")
}

generator kysely {
  provider     = "prisma-kysely"
  output       = "../src/shared/db/types"
  fileName     = "schemas.auto.ts"
  enumFileName = "enums.auto.ts"
  dbTypeName   = "TusssDb"
  camelCase    = true
}
