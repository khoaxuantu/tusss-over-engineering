datasource db {
  provider = "postgresql"
  url      = env("DB_URL")
}

generator kysely {
  provider     = "prisma-kysely"
  output       = "../src/db/types"
  fileName     = "schemas.auto.ts"
  enumFileName = "enums.auto.ts"
  dbTypeName   = "TusssDb"
}
